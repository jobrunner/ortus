# Prometheus for the per-ticket dev stack. Unlike the loadtest config (which
# scrapes one native ortus on the host), this auto-discovers every ticket's ortus
# container on the ortus-dev network via the Docker API and scrapes its metrics
# port. New `make dev-new` environments appear automatically; destroyed ones drop.
global:
  scrape_interval: 2s
  evaluation_interval: 5s

scrape_configs:
  - job_name: 'ortus-tickets'
    docker_sd_configs:
      - host: unix:///var/run/docker.sock
        filters:
          - name: label
            values: ["ortus.metrics.scrape=true"]
    relabel_configs:
      # Keep only containers that opted in via the compose label.
      - source_labels: [__meta_docker_container_label_ortus_metrics_scrape]
        regex: "true"
        action: keep
      # One target per container: pick the metrics port (9090), drop the others.
      - source_labels: [__meta_docker_port_private_number]
        regex: "9090"
        action: keep
      # Scrape <container-ip>:<metrics-port> on the ortus-dev network.
      - source_labels: [__meta_docker_network_ip, __meta_docker_container_label_ortus_metrics_port]
        separator: ":"
        target_label: __address__
      # Human-friendly labels: which ticket / container the series belongs to.
      - source_labels: [__meta_docker_container_label_ortus_metrics_ticket]
        target_label: ticket
      - source_labels: [__meta_docker_container_name]
        regex: "/(.*)"
        target_label: container
    metrics_path: /metrics
